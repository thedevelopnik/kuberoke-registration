import express, { Express, Router } from 'express'
import logger from '../logger'
const reg = express()



export function routeRegistration(router: Router): void {
  router.post('/registration/register', async function(req, res) {
    try {
      
    } catch (err) {
      
    }
  })
}
